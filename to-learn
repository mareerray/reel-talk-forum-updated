✅ SPA (Single Page App)
- https://tutorialzine.com/2015/02/single-page-app-without-a-framework

✅ Bootstrap
- https://getbootstrap.com/docs/3.4/javascript/

✅ Handlebars.js
- https://tutorialzine.com/2015/01/learn-handlebars-in-10-minutes

✅ DataBase
- https://www.sqlitetutorial.net/

✅ Makefile
 "A Makefile is a configuration file used by the make utility to automate tasks, especially in software development. 
 A Makefile can include commands to create or drop databases, load schemas, and populate test data. For example:
     - make database: Creates the database and its tables."

✅ schema.sql
"The schema.sql file is a SQL script that defines the structure of a database. It typically includes commands to create tables, define relationships, set constraints, and configure other database objects."

✅ Web Sockets
- https://medium.com/wisemonks/implementing-websockets-in-golang-d3e8e219733b

✅ Git instruction
To merge your branch (maire) with your friend's branch (fateme), follow these steps:

- Switch to your branch (maire):
```
git checkout maire
```
- Fetch the latest changes from the remote repository:
```
git fetch origin
````

- Merge your friend's branch (fateme) into yours:
```
git merge origin/fateme
```
If there are no conflicts, the merge will complete successfully. If conflicts occur, Git will notify you. Resolve them manually in the affected files, then:
```
git add .
git commit -m "Resolved merge conflicts"
````

- Push the merged branch back to GitHub:
```
git push origin maire
````

Now, your maire branch contains the updates from fateme. If you were planning to merge fateme into main, let me know, and I can guide you on that too! 🚀